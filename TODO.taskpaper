Presto:

- Add delegators for more complex routing
	`add_delegate($regex, fn), 
	`delegate(ctx)`
- Content type filter to specify that a route only handles $x
	`$this->add_contentType('json', true /* exclusive (default) */);`
- Wrap Simple SSO library in a more reusable package

