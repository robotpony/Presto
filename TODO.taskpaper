Presto:

- Add sub-delegators based on rx::v2 pieces ... self::add_delegate('//', fn)
- Add type handler registration for registering adaptors ... self::add_type_handler('text-html', fn); // or class?
- Wrap SSO library in a more sensible form

- Output adapters
